# Go toolchain
coverage.out
coverage.html
*.test
*.cpuprofile
*.heapprofile
go.work
go.work.sum
report.json
*__debug_*
bin/

# Common in OSs and IDEs
.idea
.vscode
.DS_Store
.kiro*
.claude*

# Vim swap files
*.swp
*.swo
*~

# shencore golden corpus: the full harvest is large and regenerable via
# `make record-corpus`. A curated, committed sample lives in
# test/parity/corpus-sample/ and is what CI replays.
/test/parity/corpus/
